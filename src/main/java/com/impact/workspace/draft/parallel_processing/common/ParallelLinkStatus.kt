package com.impact.workspace.draft.parallel_processing.common

enum class ParallelLinkStatus {
    SUCCESS,
    MACHINE_OFFLINE,
    NO_INPUT_HATCH,
    NO_COMMS_NETWORK,
    NO_MATCHING_HATCH,
    HATCH_ALREADY_IN_USE,
}
